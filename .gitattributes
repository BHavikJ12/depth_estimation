# Keep line endings sane across Linux/Windows checkouts.
* text=auto

# Windows batch files must have CRLF or cmd.exe mis-parses them.
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must have LF or bash fails with "bad interpreter".
*.sh text eol=lf
